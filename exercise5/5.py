ts = 0 
c  = 0

while true:
  n = int(input("введете неотрицательное число (0 для завершения):"))
  if n == 0 
      break
  ts += n
  c  += 1

if  c  > 0 :
     a = ts / c 
     print(f"среднее значение элементов последовательности: {a:.2f}")
else:
  print(не было введено ни одного числа .")
 
